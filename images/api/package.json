{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run migrate && nodemon src/index.js",
    "migrate": "npx knex migrate:latest --cwd src/db",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:latest": "npx knex migrate:latest"
  },
  "author": "dina bouazzafen",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^2.5.1",
    "node": "^20.7.0",
    "pg": "^8.11.3",
    "postgresql": "^0.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
